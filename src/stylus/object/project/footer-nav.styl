.p-footer-nav {
  bs-font-size-line-height(14px);

  &__list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  &__item {
    margin: 0;
    bs-padding(0, 0, .5);

    a {
      text-decoration: none;
    }
  }
}
