.c-section {
  _padding(3, 0);

  &--bg {
    background-color: color-main;
  }

  &__title {
    _margin(0, 0, 2);
    _font-size-line-height(_base-font-size * 2);
    text-align: center;
  }

  &__content {
    _margin-between-children(1);
  }
}
