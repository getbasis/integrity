.{_prefix}c-entries {
  _list-unstyled();

  &__item {
    _margin(1, 0, 0);

    &:first-child {
      margin-top: 0;
    }
  }

  &__item-date {
    display: block;
    font-weight: bold;
  }

  .{_prefix}c-media {
    &__figure {
      max-width: 25%;
    }
  }
}
